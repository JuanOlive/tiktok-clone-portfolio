import { MoreHorizontal } from "lucide-react";

export default function MoreConfig() {
            <button className="hover:bg-[#262626] p-1 rounded-md pr-[100px] text-white font-bold transition duration-300 flex items-center gap-2">
                <MoreHorizontal size={34} className="p-1" fontSize={33} />
                <span>mais</span>
            </button>
}